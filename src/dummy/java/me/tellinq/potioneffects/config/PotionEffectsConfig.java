package me.tellinq.potioneffects.config;

public class PotionEffectsConfig {
    public static PotionEffectsConfig INSTANCE; // Adds the instance of the mod, so we can access other variables.
    public void openGui() {}
}
